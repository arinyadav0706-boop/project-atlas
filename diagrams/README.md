# Diagrams

Mermaid source diagrams referenced from `docs/`. Prefer embedding Mermaid
directly in the relevant doc (as done in `01_Architecture/`) so diagrams
stay next to the prose they illustrate; use this folder only for diagrams
too large/shared to embed in a single doc (e.g., the full ER diagram
authored in Phase 2, and any cross-module sequence diagrams).

Planned for Phase 2:

- `er-diagram.mmd` — full entity-relationship diagram (see `docs/03_Database/`)
- `sequence-*.mmd` — cross-module sequence diagrams as needed
