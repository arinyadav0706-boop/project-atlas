// Placeholder — the real Dashboard module (assigned issues, recent
// activity, project shortcuts per docs/02_Modules/02_dashboard.md) is
// built in Phase 4. This page exists in Phase 3 only to prove the
// authenticated app shell renders correctly end-to-end.
export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-lg font-semibold text-foreground">Dashboard</h1>
      <p className="mt-2 text-sm text-muted-foreground">
        Coming in Phase 4 — see docs/02_Modules/02_dashboard.md.
      </p>
    </div>
  );
}
