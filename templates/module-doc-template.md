# Module: <Module Name>

**Status:** Draft | Reviewed | Approved
**Owner:** <role>
**Last Updated:** YYYY-MM-DD

## Overview

One paragraph: what this module does and why it exists, in the context of
the overall product (link back to `docs/00_Product/02_Product_Requirements.md`).

## Business Rules

Numbered rules (e.g., `BR-1`, `BR-2`) governing this module's behavior.

## Database

Entities owned or primarily used by this module. Link to
`docs/03_Database/` for the canonical schema; do not redefine it here,
reference it.

## API

Endpoints this module exposes/consumes. Link to `docs/04_API/openapi.yaml`
for the canonical contract.

## UI

Screens/components this module owns. Link to `docs/05_UI/` for the
canonical spec; note module-specific interaction details here.

## Acceptance Criteria

Given/When/Then style criteria that define "done" for this module.

## Validation

Input validation rules (Zod schema shape, constraints) for this module's
create/update operations.

## Future Scope

Explicitly deferred capabilities for this module (V2+), so it's clear what
was considered and intentionally excluded rather than overlooked.
