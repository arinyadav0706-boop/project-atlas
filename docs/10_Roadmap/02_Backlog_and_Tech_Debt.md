# 02 — Backlog & Tech-Debt Ledger

**The single source of truth for everything we consciously deferred.** The
Roadmap (`01_Development_Roadmap.md`) tracks *modules and phases*; this tracks
the cross-cutting items — performance polish, security, infra, testing gaps, UX,
and product decisions — that would otherwise get lost between modules.

## Discipline (non-negotiable)
- **Every time we say "later", it gets an entry here** — same commit, ideally.
- Nothing ships to production (Internal GA, Phase 8) with an open **🚩 Go-live**
  item. That column is the launch gate.
- When an item is done, mark it `DONE` with the date (don't delete — the history
  is the record). Detailed rationale lives in the linked source doc; this is the
  index.

**Priority:** P1 critical · P2 high · P3 medium · P4 scale-later.
**🚩 Go-live** = must be resolved before real users (Phase 8).

---

## 🚩 Go-live blockers (the launch checklist)

| ID | Item | Pri | Status | Notes |
|---|---|---|---|---|
| GL-1 | **Remove or rotate the seeded known-password accounts** (`arin…` + 5 teammates) | P1 | PARTIAL (code done) | **Code fixed:** `prisma/seed.ts` no longer hardcodes any credentials — the admin comes from `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` (env), and the shared-password demo team seeds only in non-prod / when no real admin is being bootstrapped (auto-off when `SEED_ADMIN_EMAIL` is set; `NODE_ENV=production` + `SEED_DEMO=false` guarantees it). Refuses to seed prod without `SEED_ADMIN_EMAIL`. **Still needs prod access:** rotate/remove the accounts already seeded into the prod DB (the old `arin…` + 5 teammates), and set `SEED_ADMIN_*` in the prod env. |
| GL-2 | **Security review** of the whole surface (Phase 7) | P1 | OPEN | Roadmap Phase 7. Run `/security-review` + manual pass. |
| GL-3 | **Rate limiting** on auth + mutation endpoints | P1 | ✅ DONE 2026-07-29 | **DB-backed fixed-window limiter (ADR-0028, finding F1):** portable (plain Postgres, no new infra), atomic under concurrency (`rate_limits`, `INSERT … ON CONFLICT … RETURNING`). **Applied everywhere:** login (per IP+email, 8/15min — fails like an invalid credential), search (per user, 60/min), and **every mutation route** (per user, 120/min) via the `requireMutationActor()` guard — so a new write route gets throttling by construction, not by remembering. `RateLimitError`→429+`Retry-After`. |
| SEC-4 | **Session revocation** (deactivation/role change effective immediately) | P1 | ✅ DONE 2026-07-29 | **Finding F2 (ADR-0029):** `getActor()` now re-reads live `{isActive, orgRole, organizationId}` on every request (one cached PK read), so a deactivated user is revoked and a role change applies on the **next request** — not on 30-day JWT expiry. Session `maxAge` cut 30d→12h (rolling 1h). Guards `requireActor()`/`requireMutationActor()` funnel all routes through the recheck; layout bounces revoked sessions. |
| SEC-5 | **Dependency vulnerabilities** (finding F3) | P1 | ✅ DONE 2026-08-08 | Criticals cleared 2026-07-29 (`next-auth` beta.32 + `@auth/core` 0.41.3). The remaining 5 highs needed the framework major and are now gone: **`next@16.3.0` + `react@19.2.8` + `eslint@9`/`eslint-config-next@16`** (ADR-0039). `npm audit` reports **0 vulnerabilities**. Verified: tsc, eslint (0 errors), 738 unit tests, prod build, and a booted server — `/sign-in` 200 with rendered HTML, `/api/users/me` 401, so next-auth works on React 19. |
| GL-4 | **Apply DB migrations to production Supabase** (incl. `perf_indexes`, `board_rank`, `rank_collation`, `rank_unique`, `issue_version`, `home_personalization`, `backlog_index`, `sprint_position`, `comments_extensible`, `labels_components`, `search_fts`, `feature_flags`) | P1 | 🚩 | OPEN | Prod schema was created manually → no migration history. **`20260723140000_feature_flags`** creates the `feature_flags` table (per-org flag overrides, ADR-0023) — purely additive, safe to run anytime; with no rows every flag takes its code-registry default, so the app behaves identically until an admin sets an override. **`20260723130000_search_fts`** creates two `GIN` expression indexes (`issues_fts_idx`, `projects_fts_idx`) over `to_tsvector('english', …)` — purely additive, safe to run anytime; without them search still works but scans instead of using the index. Search will not use them unless the expression matches byte-for-byte (it does — same string in `search.repository.ts`). **`20260723120000_labels_components`** creates `components` + `issue_components`, and swaps the labels exact-unique for a case-insensitive functional partial unique index — additive except the index swap (safe: the new index is stricter only in case-folding; check for existing case-dup label names in prod first). **`20260721100000_comments_extensible`** adds `CommentBodyFormat` enum + `comments.bodyFormat/editedAt/version` + index swap — additive, safe anytime. **`20260721000000_sprint_position`** is a plain additive `ALTER TABLE "sprints" ADD COLUMN "position" INTEGER NOT NULL DEFAULT 0;` — safe to run anytime. Baseline via `prisma migrate resolve --applied`, then `migrate deploy`. **✅ DONE 2026-07-28:** prod baselined (all 13 migrations recorded in `_prisma_migrations`; `boardOrder` column dropped to finish `board_rank`; a stray failed `perf_indexes` row from a racing auto-deploy was marked applied), and `vercel-build` (`prisma migrate deploy` before build) is live on `main` — every deploy now applies pending migrations automatically. Incident class GL-4/DB-2 closed. **Inspection scripted:** `scripts/prod-baseline-inspect.sql` (read-only). `20260719000000_board_rank` backfills `rank` in SQL for ≤62 issues per (project,status) column and **fails loudly** above that — if a prod column is larger, backfill via `generateNKeysBetween` first. `20260720000000_rank_collation` pins `rank` to `COLLATE "C"`. `20260720100000_rank_unique` adds the unique `(projectId,status,rank)` index — **check for pre-existing duplicates first** (query in the migration). **Interim:** `board_rank` + `rank_collation` were applied to prod as standalone hotfix SQL on 2026-07-20 (see incident, DB-2); the baseline must reconcile migration history with that manual state (and the unique index still needs applying). |
| GL-10 | ~~GitHub Actions stopped running — every merge since 2026-08-06 16:20 UTC is ungated~~ | P1 | ✅ RESOLVED 2026-08-08 | Actions resumed on its own. Verified by run history rather than by the absence of a banner: `lint-and-test.yml` and `build.yml` both ran and **succeeded** on `4a5b2fd` (push/main, 13:33 UTC) and on `8e9347b` (push/main, 18:09 UTC), and `seed-verus.yml` ran twice successfully the same day. Consistent with the original diagnosis — exhausted billing-cycle minutes, which reset. **The gap it left stands as history:** commits merged between 2026-08-06 16:20 and 2026-08-08 13:33 were never CI-gated; if anything odd surfaces, suspect that window. **Worth keeping:** the failure mode was silent — the PR page showed a green Vercel check while no test had run — so a red/absent check is not the signal. Check the Actions tab for *recency*, not just for green. |
| GL-5 | **Confirm `DATABASE_URL` = `?pgbouncer=true&connection_limit=1`** | P2 | PARTIAL | `pgbouncer=true` confirmed; add `&connection_limit=1`. |
| GL-6 | **SSO credentials** (Google + Microsoft OAuth apps) if launching with SSO | P2 | OPEN | Config, not code. Credentials login works today. |
| GL-7 | **Load test to ~60 concurrent** (Phase 7 NFR) | P2 | OPEN | Validate the scale targets in `05_Performance_and_Scalability.md`. |
| GL-9 | **VERUS demo data must be absent from any client-handover database** (ADR-0033) | P1 | 🚩 OPEN | The VERUS demo org (~150 users, ~8k issues) is seeded via `prisma/verus/` into our **demo/staging** Supabase only, for stakeholder demos + at-scale dogfooding. It is one deletable org (id `verus-demo-org`). **Handover rule:** a client's database must never have it — either hand over a fresh DB (never run `seed:verus` against it) or run `npm run seed:verus:teardown` first. The seed refuses to run without `SEED_VERUS=confirm` and only ever touches the VERUS org. Verify at handover: `SELECT count(*) FROM organizations WHERE id='verus-demo-org';` must be `0`. **Seeding is manual only** (SEED-5 — a weekly cron was briefly added, then removed), so a one-time teardown at handover is sufficient: nothing re-creates the org on its own. |
| GL-8 | **Configure `STORAGE_*` env in prod** for Attachments (ADR-0017) | P1 | ✅ DONE 2026-07-28 | Prod now runs `STORAGE_PROVIDER=supabase` with a private `attachments` bucket + `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_STORAGE_BUCKET` set in Vercel; attachments + avatars persist across deploys. *(Original context:)* Attachments defaults to `STORAGE_PROVIDER=local` (on-disk), which is **ephemeral on serverless** — files vanish between deploys. Prod must set `STORAGE_PROVIDER=supabase` + `SUPABASE_URL`/`SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_STORAGE_BUCKET` (or a future S3/GCS/Azure adapter). **No migration** — the `attachments` table already exists; this is config only. **Setup (do in prod):** create a **private** Supabase Storage bucket named `attachments`; set `STORAGE_PROVIDER=supabase`, `SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY` (server-only), `SUPABASE_STORAGE_BUCKET=attachments` in the Vercel env — all documented in `.env.example`. Avatars (module 16) use the same seam, so this also fixes avatar persistence. Verify by uploading an attachment + a profile avatar in prod and reloading after a redeploy. |

---

## Performance & scalability (source: `docs/01_Architecture/05_Performance_and_Scalability.md`, `docs/08_Testing/02_Performance_Investigation.md`)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| PERF-1 | Region mismatch (Vercel↔Supabase) | P1 | — | ✅ DONE 2026-07-19 | Vercel → Mumbai; reads dropped to ~1–2 s. |
| PERF-9 | Notification history index does not cover its sort | P3 | No | ✅ DONE 2026-08-08 | `notifications` had only `(userId, isRead)`, which serves the bell poll but cannot satisfy the history page's `(createdAt, id)` sort — Postgres filtered, then sorted. Added `(userId, createdAt, id)` in the ADR-0038 migration, as planned: it rode along with the change that multiplies notification rows rather than being a migration on its own. |
| PERF-2 | Create: remove full-page `router.refresh()` → in-place insert | P2 | — | ✅ DONE 2026-07-19 | Create ~8 s → ~200 ms. |
| PERF-3 | Same in-place pattern for **edit / transition / delete** | P2 | No | OPEN | These still full-refresh; make them instant like create. |
| PERF-4 | Login: 3× `findByEmail` → 1× | P3 | No | OPEN | Redundant lookups (`auth-config.ts`). |
| PERF-5 | Parallelize `resolve()` (getContext ‖ getMemberRole) | P3 | No | OPEN | Two serial queries → one parallel step. |
| PERF-6 | Drop extra relation SELECTs in `createWithKey` | P3 | No | OPEN | Fetch assignee/reporter only when needed. |
| PERF-7 | Cold start (Vercel Free/Hobby + Prisma init ~1.5–2 s) | P2 | No | OPEN | First action after idle. Fix = paid tier / keep-warm. |
| PERF-8 | Virtualize long lists (`@tanstack/react-virtual`) | P3 | No | OPEN | When page sizes climb. |
| PERF-9 | Structured logging + observability | P2 | No | OPEN | Only `console.error` today. |
| PERF-10 | Move audit-log writes off the request hot path (queue) | P3 | No | OPEN | At volume. |
| PERF-11 | Home: streamed `<Suspense>` widgets, capped + parallel | P2 | — | ✅ DONE | Home module — each section its own streamed Suspense, bounded, one membership query per request (ADR-0012). |
| PERF-12 | Read replicas for read-heavy endpoints | P4 | No | OPEN | Large scale. |
| PERF-13 | Move `RecentItem` upserts off the request hot path (queue) | P3 | No | OPEN | Home engagement signal is recorded synchronously (best-effort, guarded) on issue view/edit; queue it at volume — same treatment as audit (PERF-10). |
| PERF-14 | Workload: move aggregation SQL-side if teams outgrow app-side grouping | P4 | No | OPEN | ADR-0034. `WorkloadService.aggregateTeam` fetches one team's open issues then groups their work logs in the application — two bounded reads, fine at ~10–15 people per team (a few hundred issues). If teams get much larger, replace with one `SELECT … GROUP BY assigneeId` aggregate; the DTO and the pure `capacity.ts` maths stay unchanged. Note the time-phasing pass (ADR-0035) is per-issue and in-memory, so it would need to stay app-side or move to a second query. |
| WL-1 | Workload: per-person capacity (part-time, leave calendar) | P3 | No | OPEN | ADR-0034 BR-5 deliberately uses a code constant `WEEKLY_CAPACITY_MINUTES` (40 h) instead of inventing a `User` column (rules 2/10). A real capacity model wants working hours, leave and part-time factors — propose the schema in a doc first, then swap the constant; the aggregation does not change. |
| UI-1 | **Shared chart kit + rebuild the velocity chart on it** | P2 | No | ✅ DONE 2026-08-06 | `docs/05_UI/03_Data_Visualisation.md` §2 audited the old velocity bars: no axis, no gridlines, bars scaled to the tallest bar (so every chart looked "full" and the shape carried no information), no average line, truncated sprint labels, issue counts reachable only via a `title` tooltip (invisible on touch), a 2px sliver for a genuine zero. Delivered as `src/shared/components/charts/` on **Apache ECharts** (ADR-0036 — the in-house SVG kit was built first, then replaced the same day so the platform has one charting standard): `echarts-core.ts` is the only import site (ESLint-enforced), `chart.tsx`/`chart-canvas.tsx` the one wrapper, `chart-theme.ts` the CSS-variable bridge, `options/` the pure tested builders. Velocity rebuilt on it — V1–V7 closed. |
| UI-2 | Workload: team distribution bar + person bar chart with a real axis | P3 | No | ✅ DONE 2026-08-06 | Two builders on the ECharts kit (ADR-0036): `distribution-bar-option.ts` (the team's status mix as one stacked bar) and `capacity-bars-option.ts` (one bar per person on a shared zero-based week axis, dashed reference lines at the 0.5 and 2 week band edges, every bar captioned so a zero-length bar reads as "no open work" rather than missing data). The per-row CSS mini-bar was removed as redundant. Chart tones and row status dots now come from the same tokens. No schema change. |
| UI-3 | Status-breakdown donut uses hard-coded hex colours | P4 | No | ✅ DONE 2026-08-06 | `report-card.tsx` `STATUS_COLORS` ignored the theme and would break a white-label deployment. Now a semantic `ChartTone` resolved from theme tokens (§6); required adding `--success`/`--warning` to `globals.css` + `tailwind.config.ts` in both light and dark. |
| WL-3 | **Time-phased workload (people × weeks grid)** | P3 | No | ✅ DONE 2026-08-06 | ADR-0035 accepted; all four open questions settled. Closed the biggest gap vs ClickUp/Asana: the view said "2 weeks queued", not "slammed next week, free the week after". Delivered as `src/features/scheduling/` — a feature module, not workload-internal, because Gantt and Calendar (V2 Epic 6) must answer "when does this work happen" identically and Jira separates one scheduling engine from the views on it. Chain: issue dates (dormant) → due date spread **from today** (Asana) → sprint window (Jira) → Unscheduled (ClickUp). No schema change. Columns: Overdue (conditional) · 4 real-dated weeks · Later · Unscheduled; cells carry hours **and** % of capacity; sprint-inferred cells marked `S` (Jira). |
| WL-4 | `Issue.startDate` so effort spreads across an explicit range | P4 | No | OPEN | ADR-0035 §6. **Reassigned to the Gantt/Timeline module (V2 Epic 6)**, which cannot exist without per-issue start dates where workload can — and adding it now would settle how issue dates and sprint dates compete with no V2 UI to validate against. Cost when it lands is genuinely one branch: rule 1 of `resolve-window.ts` is already written and dormant, and `placeIssue` passes `startDate: null` from a single line. Needs a doc + migration + form UI + a cross-field `start ≤ due` rule. |
| RP-4 | **Burndown: burnup companion + Jira-style remaining-estimate hours** | P3 | No | OPEN | Two named gaps vs. the incumbents (ADR-0037 amendment). **Burnup:** Jira and ClickUp both ship one; it is the conventional way to show scope growth, and needs the same membership history as RP-1. **Hours semantics:** Jira's time-based burndown drops as hours are *logged*; ours drops only when an issue reaches Done, so our "Hours" axis means "estimate of not-yet-finished work", not Jira's "remaining estimate". We already have `WorkLog` data to do the real thing — until then the two are different metrics wearing the same word. |
| RP-1 | **Burndown v2 — true sprint membership** | P3 | No | OPEN | ADR-0037 follow-up. v1 burns down the sprint's *current* contents because membership was never audited; `ISSUE_SPRINT_CHANGED` now accrues on `SprintService.moveIssue`, so once it has covered a full sprint the cohort can be replayed properly and scope-change markers drawn (Jira does this). Purely additive — `buildBurndown` already takes the cohort as an argument. |
| RP-2 | Audit the bulk sprint paths (`delete` → release, `complete` → move incomplete) | P4 | No | OPEN | ADR-0037 §2 deliberately audited only the single-issue move: the bulk paths fire at a sprint **boundary**, so they cannot distort a burndown drawn over `[startDate, endDate]`. Cheap to add, needed for a complete membership history rather than for correctness today. |
| RP-3 | `sprint_daily_snapshot` table + scheduled job | P4 | No | OPEN | Would make burndown exact **and** retire velocity's "history moves" caveat (Metric Definitions §3) in one change — completed points would stop changing when someone edits an old sprint. Needs a schema change, a cron and a backfill story; propose as a doc first (rule 2). |
| WL-5 | Workload grid: click a cell to list the issues behind it | P4 | No | OPEN | ADR-0035 left this out of the first cut to keep scope tight. Wants a `GET /api/workload/users/{id}/issues?week=…` under the same BR-11 scope check, and the cells become buttons (they are plain `<td>`s today, so nothing about the current markup is keyboard-hostile). |
| WL-2 | Workload: optional descendant-team roll-up + reassign from the row | P4 | No | OPEN | ADR-0034 BR-7 shows one team's direct members (a parent roll-up is unbounded and unreadable — Engineering is 91 people). Add behind an explicit toggle if managers ask. Drag-to-reassign from a workload row is the natural follow-on once roll-up exists. |

## Database & infrastructure

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| DB-1 | Covering indexes (issue list, my-issues, activity) | P2 | — | ✅ DONE (in code) | Migration `20260715000000_perf_indexes`; **apply to prod = GL-4**. |
| DB-2 | Real migration workflow (`migrate deploy` on deploy + baseline) | P1 | 🚩 | PARTIAL | **Root cause of the 2026-07-20 prod outage:** deploy ran only `next build`, so merging the Board shipped code that queried `rank` while prod still had `boardOrder` → Issues/Board 500'd. **Mechanism DONE (in code):** `vercel-build` script now runs `prisma migrate deploy` before build; runbook in `docs/06_Infrastructure/03_Migrations_and_Deploy.md`. **Still open (needs prod access):** the one-time prod baseline (inspect → apply-missing → `migrate resolve --applied` all migrations). `vercel-build` must NOT reach a deploying branch until that baseline is done, or new builds fail (running app stays up). Closes GL-4 when complete. |
| DB-6 | `rank` column pinned to `COLLATE "C"` (byte order) | P1 | — | ✅ DONE (in code) | Migration `20260720000000_rank_collation` + collation integration guard. LexoRank keys only sort correctly under byte order (ADR-0009); a locale collation silently breaks board order. **Apply to prod = part of GL-4** (or the standalone hotfix SQL already run). |
| DB-3 | Partial index `WHERE deletedAt IS NULL` on issues | P3 | No | OPEN | Not expressible in Prisma schema; raw SQL migration. |
| DB-4 | `CREATE INDEX CONCURRENTLY` at scale | P4 | No | OPEN | Ops runbook for large tables. |
| DB-5 | `audit_logs` / `notifications` retention & partitioning | P4 | No | OPEN | Unbounded-growth tables. |
| DB-8 | Reorder/edit concurrency hardening | P3 | No | PARTIAL | **Collision-free keys DONE (ADR-0010).** **Same-card lost-update DONE for reorder, edit, and transition (ADR-0011):** `version` column + conditional writes (`reorderWithVersion`/`updateWithVersion`/`setStatusWithVersion`); a stale change gets a `409` instead of silently overwriting; unit + integration + route tests; clients (board drag, edit dialog, status control) send and refresh the version. **Still open:** (a) OCC on **delete** (currently increments version but isn't version-checked — deleting is terminal, lowest value); (b) on-demand re-rank **repair tool** for any fractional-key clusters from a prior collision. |
| DB-7 | Multi-engine SQL support (MySQL/SQL Server/Oracle) | P4 | No | PARKED (V2/V3) | **Not a current goal** — EAGLES is PostgreSQL-only by design (ADR-0002/0004). Accommodatable in this repo (repository pattern shields app code), not a rewrite, but a scoped initiative: rewrite raw-SQL migrations per engine, replace Postgres-only features (enums, FTS, `pg_trgm`, RLS, `COLLATE "C"`), add a per-engine CI matrix. Requires its own ADR + cost estimate first. Surface catalogued in `docs/01_Architecture/06_Portability_Boundary.md`. |

## Security & tenancy (source: `docs/07_Security/`, `docs/08_Testing/01_Testing_Strategy.md`)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| SEC-1 | F-1: scope all reads/writes to caller's org | P1 | — | ✅ DONE 2026-07-19 | Cross-org access now fails closed (NotFound). |
| SEC-2 | Postgres Row-Level Security as tenant backstop | P3 | No | OPEN | Defense-in-depth; recommended before multi-tenant SaaS (V2). |
| SEC-3 | (see GL-1, GL-2, GL-3) | — | 🚩 | OPEN | Tracked in the go-live table. |

## RBAC / product decisions (tracked, deferred by decision)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| RBAC-1 | Org ADMIN as project admin | — | No | ✅ RESOLVED 2026-07-23 (ADR-0024) | **Reversed the 2026-07-12 decision.** Org `ADMIN` is now an **effective `LEAD` on every project in its own org**, via the single `elevate()` in the permission engine (`15_roles.md` BR-2). Authorization-only (no membership rows), org-scoped (F-1 intact), and excluded from the "≥1 real LEAD" guard. Proven by `permission.integration.test.ts`. |
| RBAC-2 | Lone-lead handoff gap | P3 | No | OPEN | A single-lead project can't transfer leadership without first adding a 2nd lead. Tied to RBAC-1. |

## Testing gaps (source: `docs/08_Testing/01_Testing_Strategy.md`)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| TEST-1 | E2E: full workflow walk (TODO→…→DONE) + "Load more" >50 | P3 | No | OPEN | |
| TEST-2 | Concurrency-on-edit test (two users transition same issue) | P3 | No | OPEN | |
| TEST-3 | Keep the RBAC matrix complete as each new module lands | P2 | No | ONGOING | Per-module acceptance criterion (`15_roles.md`). |
| TEST-4 | Board **E2E** (Playwright): drag reorder persists across reload; cross-column move runs the workflow; VIEWER drag disabled | P3 | No | OPEN | Reorder data path is covered by `board.integration.test.ts` + `board.service.test.ts` + the `rank` route test; the dnd-kit UI wiring (neighbour computation on drop) has no browser test yet. |
| TEST-5 | Backlog **E2E** (Playwright): drag reorder persists across reload; VIEWER drag disabled; "Load more" pages | P3 | No | OPEN | Data path covered by `backlog.integration.test.ts` + `backlog.service.test.ts` + the shared `rank` route test (scope=backlog); the dnd-kit single-list UI wiring has no browser test yet (same gap as TEST-4). |
| TEST-6 | Sprint **E2E** (Playwright): drag Backlog↔Sprint; Start/Complete/Delete flow; VIEWER read-only | P3 | No | PARTIAL | `e2e/sprint-drag.spec.ts` drives a real browser through create → start → complete → history → delete, and drag of a backlog **card body** into the sprint. Caught DND-1 (whole card not draggable) and the stale-state-after-complete bug. Still to add: VIEWER read-only + within-sprint reorder in the browser. |
| TEST-7 | **VERUS seed created zero labels** — every label path unexercised against real data | P2 | No | ✅ FIXED 2026-08-08 | Labels shipped complete (schema, service, RBAC, chips, filter) and the demo generator never made one, so the chip row, the `label` filter and the `deletedAt` guard in `issueCardSelect` had only fixtures behind them. Fixed: `LABELS` pool in `prisma/verus/data.ts` (12 org-scoped, cross-cutting), independent weighted roll per label with a per-issue-type affinity table, so 64% of issues carry ≥1 and the distribution stays uneven (`tech-debt` 1145 → `compliance` 180). `src/features/labels/labels.seed.test.ts` replays the real generated dataset. Same failure mode as the burndown flat line (RP-0): green tests over an empty table. |
| DND-1 | Whole card must be the drag handle; kill native anchor drag | P2 | — | ✅ FIXED 2026-07-21 | Backlog/sprint rows only made the tiny grip draggable, and the title `<a>` triggered the browser's **native link-drag** (a URL ghost) that hijacked the gesture — users "couldn't drag" issues into a sprint. Fix: whole row is the drag handle + `draggable={false}` on the title link (backlog-item + board-card). Regression-guarded by `e2e/sprint-drag.spec.ts`. Root lesson: green unit/integration + a handle-targeted e2e passed while the real user gesture failed — **drive the actual gesture in a browser**. |

## UX / UI

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| UX-1 | **Premium UI re-skin** of the whole app | P2 | No | PLANNED | Deliberately "basic UI until MVP complete", then premium pass (founder manifesto). Sign-in already premium (the one exception). Decoupled from data → no migration needed. |
| UX-2 | Board ordering scheme | P2 | No | DECIDED (ADR-0009) | String fractional ranking (LexoRank-style, `rank` column via `fractional-indexing`) — adopted now while data volume is near-zero. Board build: migration `boardOrder`→`rank` + backfill, `createWithKey` append, `generateKeyBetween` on reorder. Supersedes the float decision (ADR-0007). |
| UX-4 | ~~Per-column **rebalance** utility for `boardOrder`~~ | — | No | RETIRED | Not needed: string fractional ranking has no precision ceiling (ADR-0009 supersedes ADR-0007). |
| UX-5 | Board per-column "load more" (very large columns) | P3 | No | OPEN | Columns are capped at `BOARD_COLUMN_LIMIT`=100 in V1 (ADR-0008 / Perf doc). At the cap the lowest-ranked tail is omitted; add per-column keyset "load more". |
| UX-6 | Board **live cross-column drag preview** (dnd-kit `onDragOver`) | P3 | No | OPEN | V1 re-lays columns optimistically on **drop** (`onDragEnd`) only — the drag itself doesn't show the card crossing columns live. Fine for "basic UI"; polish with the premium pass (UX-1). |
| FUT-3 | Board **Saved Filters** (stored named `BoardFilter`) | P3 | No | OPEN | Reuses the ADR-0008 filter contract; future table. |
| FUT-4 | Board filters not yet activated: Sprint, Epic, Label | P3 | No | ✅ DONE 2026-08-07 | All of them are live. Epic landed with ADR-0026, Labels/Components with ADR-0018, and **Sprint** completed the set — the `BoardFilter` contract and server `where` had accepted it since ADR-0008, so it was a control, not a redesign, exactly as that ADR predicted. The select renders only when the project has sprints. |
| UX-3 | Consistent empty / loading / error states pass | P3 | No | ✅ PARTIAL 2026-08-08 | `EmptyState` primitive added and applied across Home; the three sections that returned `null` when empty now render a real state, which the two-column grid requires. Other surfaces still hand-roll theirs. |
| UI-1 | **Design-system primitives** (Card/StatTile/EmptyState/PageHeader) | P2 | No | ✅ DONE 2026-08-08 | Replaces hand-copied `rounded-xl border border-border` in 5+ components. Elevation as three named steps, surfaces softer than controls, tints from semantic tokens only. See `05_UI/01_UI_Design_Principles.md`. |
| UI-2 | **Apply the system beyond Home and Workload** — Board, Backlog, Issues, Projects, Admin | P2 | No | ✅ DONE 2026-08-10 | All 17 app routes converted and screenshot-verified against a locally seeded VERUS database. Added `PageShell` (two widths, replacing four ad-hoc ones) and `TabNav` (replacing two divergent tab implementations); every page now uses `PageHeader`. Panels became cards; kanban columns and sprint drop zones became recessed wells inside cards; comment/attachment/issue rows deliberately stayed flat (one level of nesting — see `05_UI/01_UI_Design_Principles.md`, "Where cards stop"). Issue detail was the last page still bare on the canvas and now carries three sheets plus a metadata rail. Two regressions caught by rendering, not by tsc: dropping `"use client"` from `AdminConsoleNav` and then `ProjectTabs` passed lucide component references across the server/client boundary and 500'd every admin route, then every project route. |
| UI-10 | **Verification harness must assert on the page, not the status code** | P3 | No | ✅ DONE 2026-08-10 | The screenshot script reported `200` for all 17 routes while every shot was the sign-in page (an unauthenticated bounce renders 200), and later reported `200` for a route showing the error boundary (which also renders 200). It now asserts the post-login URL left `/sign-in` and that "Something went wrong" is absent. Worth keeping in mind for the E2E suite: HTTP status is not a liveness check in an app that renders its failures. |
| UI-3 | **Workload page visual pass** | P2 | No | ✅ DONE 2026-08-10 | Rebuilt on the primitives per the approved mockup: stat tiles, team-mix donut with a DOM legend, People at a glance, Project balance (new — BR-16), Overloaded/Has-room lists that expand the person's row in All people, per-person bars unchanged. Omitted from the mockup, each with a reason: **AI Insights** and **Upgrade to Pro** (neither exists — EAGLES has no AI feature and no paid tiers); **tile sparklines and deltas** (UI-4); **Filters / date-range chips** (would be controls that filter nothing — the real controls are the team picker and the By person/By week toggle); **avatars on the chart's y-axis** (canvas — see 21_workload.md §5). Replaced `distribution-bar-option.ts` with `ring-donut-option.ts`; the stacked bar had one caller and the mockup asks for a ring. |
| UI-6 | Workload: **"View unestimated issues"** link on the coverage banner | P3 | No | ✅ DONE 2026-08-10 | Closed by the Saved Views module (ADR-0040), which is what it was always waiting on: a cross-project list and a filter that can say "has no estimate". The link is `/issues?hasEstimate=false&openOnly=true`. `openOnly` was added because the banner counts OPEN unestimated issues — without it the destination showed finished work and quietly disagreed with the number that sent you there (caught in the browser, 21 Done rows). |
| UI-4 | **Month-over-month trend deltas on Workload tiles** | P3 | No | OPEN | Mockup shows "↑2 from last month". Feasibility differs per figure: **people** and **open issues** are computable truthfully (user `createdAt`; status replay from the audit log, the burndown technique). **Work remaining** and **overloaded** are not — they depend on estimates *as they were then*, and `estimateMinutes` is not versioned, so a past figure would silently use today's numbers. Either version estimates or ship only the two honest deltas. |
| UI-7 | **Canvas/surface separation + shell scroll fix** | P2 | No | ✅ DONE 2026-08-10 | Two defects found by rendering the app in a headless browser against a locally seeded VERUS database, not by reading CSS. (1) `--background` was pure white for *both* the page and the cards, so the three elevation steps were invisible and the app read flat against the mockup — added `--canvas` (light: tinted; dark: darker than the card, relationship inverted) and moved `<main>` onto it; chrome stays white. (2) The app shell had **two scrollbars**: flex children default to `min-height:auto`, so the column would not shrink below its content and the document scrolled behind `<main>`'s own scroller, carrying the top bar off screen (`documentElement.scrollHeight` 955 vs a 900px viewport). Fixed with `min-h-0`/`min-w-0`. Also right-aligned the top-bar cluster, which had been stranded mid-window. |
| UI-8 | **Workload detail routes** — `/workload/people`, `/workload/projects` | P2 | No | ✅ DONE 2026-08-10 | The dashboard's "View all" links scrolled to an anchor at the bottom of the same page. Measured cost: 17 members made `/workload` **3.1 viewport-heights** tall, so the charts were below the fold, and the scroll was indistinguishable from the page jumping. Full lists moved to their own routes with a back link and `?teamId=` carried through; `?person=` deep-links a row open. Dashboard is now **1.4** screens. Chart also stopped plotting the 5 people with no queued work (a third of its height on empty tracks). Established as a general rule in `05_UI/01_UI_Design_Principles.md` — "Detail routes, not in-page anchors" — and it is the pattern for every future "View all". |
| UI-9 | **Workload legend/tile text was truncating** | P3 | No | ✅ DONE 2026-08-10 | Team mix rendered every legend entry ellipsised ("Overl… 0 0%") — a legend that had stopped being a legend — and the People-at-a-glance labels wrapped to one, two and three lines, giving four tiles three different baselines. Ring narrowed to leave the legend its natural width; glance labels sized to the column with `whitespace-nowrap`. Both were visible in the first screenshot of the page and invisible in the source. |
| CF-1 | **Custom fields** (V2 module 24) | P2 | No | ✅ DONE 2026-08-10 | Org-level field library + per-project enablement + values on issues. Eight types (TEXT/NUMBER/DATE/CHECKBOX/SELECT/MULTI_SELECT/USER/URL). **Typed EAV, not JSONB** (ADR-0042 §1): values are user data that must outlive the field, stay indexable, and keep referential integrity for select options — a rename must not orphan thousands of strings. **Asana's model, not Jira's contexts** (§2): one definition per concept so cross-project reporting stays possible, opt-in per project so nobody sees fields they do not care about. `Decimal(18,4)` not `Float` — a custom number is as likely to be money as a count. Type is immutable after creation; the PATCH schema refuses it rather than ignoring it. 51 unit tests plus a live run: duplicate name 409, select-with-no-options 422, type change 422, `javascript:` URL 422, foreign option id 422, error messages name the field. Verified in the DB that renaming an option preserves the value and that clearing deletes the row. |
| CF-2 | **Filter by custom fields** | P2 | No | ✅ DONE 2026-08-10 | ADR-0043. `IssueFilter` gained one open-ended key rather than a second filter language, so Board, Backlog, the cross-project list and saved views all inherit it. **The client sends a field id, never a type** — the service resolves it from the definitions, because a supplied type could aim a NUMBER field at the text column. Unknown/deleted fields and illegal operators are dropped, so a saved view outliving one of its fields still opens. **Each predicate is its own `some` clause**: merging them asks for one value row belonging to two fields, which is never true and fails as a silently empty list. Proven live — `Tier=Gold AND Value>100` returned exactly the one matching issue out of three. `is_empty` is `none`, which works only because clearing deletes the row (BR-10). `eq` on a DATE means that day. 33 new tests. |
| CF-5 | **Sort by a custom field** | P3 | No | OPEN | Prisma can order a to-many relation only by `_count`; ordering by a value inside it is not expressible — verified against the generated client (`{ customFieldValues: { valueText: "asc" } }` is a type error, `{ _count: "asc" }` compiles), not assumed. Supporting it needs a raw-SQL path with a LEFT JOIN, which would be a **second implementation of the entire filter** that has to agree with the Prisma one forever — the drift ADR-0008 exists to prevent. Filtering by a field is far more common than sorting by one, so filtering shipped alone (ADR-0043 §5). |
| CF-3 | **Required custom fields are enforced on CREATE only** | P3 | No | OPEN (by design) | ADR-0042 §4. Adding a required field to a project with 3,600 existing issues must not make all of them unsavable, and must not make bulk edit start failing for reasons the operator did not choose. The issue detail page marks a missing required value so it can be fixed deliberately. Revisit if teams ask for required-on-transition (Jira's model, and the part of Jira people complain about). |
| CF-4 | **USER custom fields are read-only in the UI** | P3 | No | OPEN | The API validates and stores them (a USER value must be a member of that issue's project), and the value displays, but the control has no people picker — it needs the project's member list, which the field control is not given. Same shape as the bulk-edit assignee gap. |
| BE-1 | **Bulk edit** (V2 module 23) | P2 | No | ✅ DONE 2026-08-10 | `POST /api/issues/bulk` + selection UI on `/issues`. **Best effort with a per-issue report**, not all-or-nothing (ADR-0041 §1): with per-project permissions and a workflow-constrained status, a 40-issue selection is routinely only partly applicable, and one archived project should not discard 39 valid edits. **No per-issue `expectedVersion`, but `version` still increments** (§2) — bulk edit opts out of CHECKING the version, never out of maintaining it, so single-issue OCC keeps working; verified in the running app. Explicit ids capped at 100, never "all matching the filter" (§3). 20 unit tests plus a live run: illegal TODO→DONE fails 3/3 with `invalid_transition`, a real change updates 6/6, re-applying skips 6/6, versions bump. |
| SV-1 | **Saved Views & cross-project issues** (V2 module 22) | P2 | No | ✅ DONE 2026-08-10 | New `/issues` route, `SavedView` table, and `GET /api/issues`. The security rule (ADR-0040 §1): project scope is resolved from the VIEWER's membership and a view's `projectIds` can only narrow it — verified in the running app, where a member of 2 projects requesting all 4 still gets 2. `IssueFilter` gained `status`, `openOnly` and `hasEstimate`; Board and Backlog share the same builder, now taking a project LIST. Filter stored as JSON, re-validated on read: a rotted filter opens empty with a warning rather than 500ing (BR-8). 30 new tests. |
| UI-5 | Time-of-day greeting assumes one timezone | P4 | No | OPEN | Home says "Good morning" from the **server** clock. Correct while the org shares a timezone with the deploy region (one company, Vercel in Mumbai). Multi-region means "Good morning" at 11pm; the fix is a client component, deferred rather than overlooked. |
| UX-7 | **Per-view field visibility** ("Fields" / "Show fields" menu) | P3 | No | OPEN | Jira (Card layout, up to 3 fields), ClickUp (Show fields) and Asana (Card appearance) all make row metadata a *user* setting; we hardcode it (`selectChips`: cap 3, labels before components, components off on list rows). Correct long-term answer to "what if an issue has ten labels" — see `06_Competitive_Landscape.md` §2b. Needs a per-user view-preference store, so it waits. |

## Demo data (VERUS) — realism debt (source: ADR-0033)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| SEED-1 | Sprint size bounded to a real team sprint | P1 | No | ✅ DONE 2026-08-08 | Was 1,229 issues in 14 days — a per-issue probability over the whole 3,600-issue pool with no cap. Now targets 42–58 with a hard ceiling of 70, derived from the expected pool per status (ADR-0033 r4). |
| SEED-2 | Size what a team commits to | P1 | No | ✅ DONE 2026-08-08 | 55% of sprint issues carried no story points, which reduced the points burndown to a floor. Now ≥90% sized inside a sprint; patchy outside one, which is true to life (ADR-0033 r5). |
| SEED-3 | Dataset clock follows the run clock | P1 | No | ✅ DONE 2026-08-08 | `NOW` was the literal `2026-08-05`, so the active sprint's burndown gained one flat day for every day since the last seed, and "due soon"/overdue/30-day windows drifted with it. Now `new Date()`; reproducibility stays with the PRNG (ADR-0033 r6). |
| SEED-4 | Active sprint carries all three states | P2 | No | ✅ DONE 2026-08-08 | Every TODO went to a *planned* sprint, so the running sprint had an empty To Do column. |
| SEED-5 | **Re-seed cadence for the deployed demo** | P2 | No | ✅ DECIDED 2026-08-08 — manual | A weekly cron was added and then removed the same day. The ageing is real — completions stop at the seed instant while the app keeps moving — but re-seeding is one click from the Actions tab, and a recurring automatic write to a production database only earns its keep if nobody would otherwise remember. **Re-seed before a demo, or when the active sprint starts looking quiet.** Side benefit: nothing to disarm at handover (GL-9). |

## Framework upgrade follow-ups (source: ADR-0039)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| UPG-1 | **Re-enable `react-hooks/refs` + `set-state-in-effect` as errors** | P3 | No | OPEN | Next 16 ships React Compiler advisory rules; 14 sites fire. Both are currently `warn` with the reasoning in `eslint.config.mjs`. Most look like false positives for our patterns — `refs` fires on dnd-kit's own `setNodeRef`, `set-state-in-effect` on the notification poller, which is what effects are *for*. Some of the rest are probably real. Deciding which is a behavioural change and does not belong inside a framework upgrade (ADR-0039). |
| UPG-2 | **Prettier is not enforced** | P3 | No | OPEN | `npx prettier --check src` reports ~167 files. Pre-existing — CI only runs `eslint`. Formatting the repo is a one-command change that would bury any real diff, so it wants its own commit, ideally with `prettier --check` added to CI in the same move. |
| UPG-3 | **Adopt Next 16 capabilities we deliberately skipped** | P4 | No | OPEN | Cache Components/PPR, Server Actions, Turbopack builds. All opt-in; ADR-0039 kept "make it work on 16" separate from "adopt 16's ideas". Each needs its own ADR if we ever want it. |
| UPG-4 | **Quarterly dependency review** | P2 | No | OPEN | Standing item per Coding Standards §11. `npm outdated` + `npm audit`, minuted here. Next due 2026-11-08. |

## Comments & Notifications (source: ADR-0016, ADR-0019, ADR-0038)

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| CMT-1 | **@mentions** | P1 | No | ✅ DONE 2026-08-08 | ADR-0038 §1–2. Structured `@[Name](user:id)` tokens bound to the immutable id, so a rename cannot re-point or break historical comments. `comment_mentions` is a derived index rebuilt on edit. **No cap** by explicit decision. `NotificationType.MENTIONED` is no longer a dead enum value. |
| CMT-2 | **Participant notifications** | P1 | No | ✅ DONE 2026-08-08 | ADR-0038 §3. Assignee + reporter + everyone who previously commented. Mention outranks participation — one notification, not two. An edit notifies only the names it added. |
| CMT-3 | **Reply threads + overflow page** | P1 | No | ✅ DONE 2026-08-08 | ADR-0038 §4–5. One level deep (a reply to a reply re-parents to the root). Issue page shows the newest 3 replies per root; past that, `View all N replies` → `/projects/{p}/issues/{i}/comments/{c}`. Keeps the issue page's cost constant however large a discussion grows. |
| CMT-4 | **Markdown rendering** | P2 | No | ✅ DONE 2026-08-10 | `bodyFormat` had promised `MARKDOWN` since the table was created while the renderer printed literal asterisks. Now a bounded subset (bold/italic/strike/code, fenced blocks, lists, quotes, links) parsed in `comments/lib/markdown.ts` and rendered as React elements. **No sanitiser was needed after all** — the original backlog note assumed one, but the parser never produces an HTML string, so the XSS boundary is unchanged by construction and there is no `dangerouslySetInnerHTML` in the feature. Hand-written rather than react-markdown because the mention token `@[Name](user:id)` IS a Markdown link to a CommonMark parser; a general parser needs a custom remark plugin, and pre-splitting on mentions breaks emphasis that spans one. Link hrefs are allowlisted (http/https/mailto/relative); a rejected link renders as literal text. 25 unit tests plus a rendered-DOM audit (no `javascript:` href, script text visible as characters). One real bug caught by test: without CommonMark's flanking rule `2 * 3 * 4 = 24` rendered as `2  3  4 = 24`. |
| CMT-5 | Reactions | P3 | No | OPEN | New table. Needs a picker and an aggregate-per-comment read. |
| CMT-6 | Comment edit history | P3 | No | OPEN | New table. `version` already increments per edit, so the revision numbering exists; the bodies are not retained. |
| CMT-7 | Comment attachments | P3 | No | OPEN | **`attachments.commentId` does not exist** — the column has to be added; earlier notes implying otherwise were wrong. |
| CMT-8 | Rich-text composer (contenteditable) | P3 | No | OPEN | The textarea shows raw `@[Name](user:id)` tokens while typing. A pill-rendering editor fixes the cosmetics and brings selection/paste/IME bugs; deferred deliberately, not overlooked. |
| CMT-9 | Mention a *group* (team) | P3 | No | OPEN | `@team-name` fanning out to members. Interacts with CMT-1's no-cap decision — a group mention makes the fan-out size implicit rather than typed, which is the case where a bound may genuinely be worth revisiting. |
| NTF-1 | Real-time push (websocket/SSE) | P3 | No | OPEN | The bell polls every 60s. `NotificationService` is the seam a transport attaches to. |
| NTF-2 | Email digest | P3 | No | OPEN | Needs a provider + unsubscribe handling. |
| NTF-3 | Per-type notification preferences | P3 | No | OPEN | Today `notificationsEnabled` is one global switch per user. Per-type opt-out (mentions on, comment-added off) is the common next ask. |

## Sprint/Backlog page — deferred UI (depends on future modules)

Catalogued from the reference planning-screen review (2026-07-21). The core
planning area (sprint sections, drag, lifecycle, row menu, star, dates, progress)
is built; these need modules we haven't shipped and are **not** blockers.

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| SP-1 | **Burndown** chart on the sprint | P3 | No | OPEN | Reports module. Needs a remaining-work time-series — reconstructable from the `audit_logs` `ISSUE_STATUS_CHANGED` events (already timestamped) or daily snapshots. |
| SP-2 | **Per-column search** (sprint backlog / backlog text search) | P3 | No | OPEN | Small: add a `search` (title-contains) param to the backlog + sprint-issues queries — mirrors the Board's `where`. Can fold into the Search module. |
| SP-3 | **Per-column filters** (type/assignee/priority/label) on backlog + sprint | P3 | No | OPEN | Reuse the ADR-0008 `BoardFilter` contract; add controls + params. |
| SP-4 | **Global search (⌘K)** in the top bar | P2 | No | OPEN | Search module (FUT-1) — cross-entity. |
| SP-5 | **Notifications** bell + unread badge in the top bar | P2 | No | OPEN | Notifications module (planned). |
| SP-6 | Two-column (single-sprint focus) layout + sprint **Details** tab | P4 | No | PARKED | Alternative to the current stacked multi-sprint layout (decided: keep vertical/stacked, ADR-0015). Revisit in the premium re-skin (UX-1). |
| SP-7 | Inline goal edit (pencil) on the sprint header | P4 | No | OPEN | We ship an edit dialog (BR-8); inline editing is a polish item for UX-1. |

## Groundwork for future modules

| ID | Item | Pri | 🚩 | Status | Notes |
|---|---|---|---|---|---|
| FUT-1 | Search: full-text strategy (`tsvector`/`pg_trgm` vs engine) | P3 | No | OPEN | Decide before the Search module. |
| FUT-2 | Attachments: implement a `StorageAdapter` (S3/Supabase/Azure/local) | P3 | No | ✅ DONE 2026-07-22 | Provider-agnostic `StorageAdapter` (ADR-0004/0017): `LocalStorageAdapter` (default, dev/self-host/tests) + `SupabaseStorageAdapter` (REST, no SDK), chosen by `STORAGE_PROVIDER` in a cached factory. Attachments MVP built on it (upload/list/RBAC-proxied download/delete). S3/GCS/Azure = a new class + factory case, no feature-code change. **Prod needs `STORAGE_*` env = GL-8** (no migration — the `attachments` table already exists). |
| FUT-5 | Sprint: **follow-up-sprint target at completion** (`moveIncompleteToSprintId`) | P3 | — | ✅ DONE 2026-07-21 | Complete offers Backlog (default) or a follow-up PLANNED sprint; incomplete issues move there keeping rank. Service + `POST /sprints/{id}/complete` body + modal picker; unit + integration + e2e. |
| FUT-6 | Sprint: **multi-sprint planning** (several `PLANNED` sprints at once) | P2 | — | ✅ DONE 2026-07-21 | ADR-0015: the Backlog page shows every non-completed sprint (ACTIVE + all PLANNED) as its own droppable section; drag issues into any of them. Panel DTO → `sprints[]`; repo `listPlanning`. Browser-verified (`e2e/sprint-drag.spec.ts`: multiple sprints, drag into a chosen one). **Remaining sub-gap → FUT-8.** |
| FUT-8 | Sprint: **reorder the planned-sprint queue** | P3 | — | ✅ DONE 2026-07-21 | Up/down controls on planned sprints; `Sprint.position` (migration `20260721000000_sprint_position` — **apply to prod = part of GL-4**), `listPlanning` orders by it, `reorderQueue` reindexes transactionally; `PATCH /projects/{id}/sprints/order`. Unit + integration + e2e. |
| SP-8 | Backlog/Sprint: **bulk select + move** | P3 | — | ✅ DONE 2026-07-21 | Checkbox per row + action bar; move all selected to a sprint or the backlog (each a version-checked move, chained to preserve order). e2e-verified. Caught a stale-closure bug in the sprint-reorder handler during browser verify. |
| FUT-7 | Sprint: **velocity / burndown reports** | P3 | No | OPEN | The `COMPLETED` sprint's issue set is the immutable record (BR-5); story-point totals already computed in progress. Consumed by the Reports module (`11_reports.md`). |
| FUT-9 | Attachments: **presigned direct-to-storage upload + signed-URL download** | P2 | No | OPEN | MVP proxies bytes *through* the app (`getObject`/upload), so the per-file ceiling is bounded by the host's request-body limit (~4.5 MB on many serverless platforms) — hence `ATTACHMENT_MAX_BYTES` defaults to 4 MB. Wiring the documented `getSignedUploadUrl`/`getSignedDownloadUrl` seams (ADR-0017 §4) lets the browser upload straight to storage (no app-server limit → true 25 MB+/file), offloads download bandwidth, and enables expiring share links. **This is the real fix for large uploads.** |
| FUT-10 | Attachments: **image/PDF previews & thumbnails** | P3 | No | OPEN | `AttachmentDto.previewUrl` is an additive field (ADR-0017). Generate on upload or on demand; no client break. |
| FUT-11 | Attachments: **versioning, dedup (hash), virus scanning** | P3 | No | OPEN | Additive columns (`version`/`hash`/`scanStatus`) + extension tables per ADR-0017 §5 — none built in MVP. Scanning gates download; dedup keys blobs by content hash. |
| FUT-12 | Attachments: **per-project storage quotas + parallel bulk upload** | P4 | No | OPEN | MVP uploads sequentially and enforces only a per-file 25 MB cap. Quota accounting + a parallel bulk path are deferred (ADR-0017 §5). |
| FUT-13 | Attachments: **comment attachments + AI document processing** | P3 | No | OPEN | `attachments.commentId` is reserved for attaching to comments; the upload event seam is the hook for AI extraction/OCR. Both deferred (ADR-0017). |
| FUT-14 | Labels/Components: **filter controls in Board bar** | P2 | No | ✅ DONE 2026-07-23 | Label + component multiselect dropdowns in the board filter bar (catalogs fetched on the board page, threaded to the bar). Backlog filter bar still pending (dep: Backlog filter UX, FUT). |
| FUT-15 | Labels/Components: **chips on board cards** | P3 | No | ✅ DONE 2026-07-23 | `labels[]`/`components[]` added as **optional** fields on `IssueListItemDto` (populated only by the board mapper — avoided the 5-mapper change). Card renders component + colored-label chips. List-row/backlog chips remain (just wire more mappers when wanted). |
| FUT-16 | Labels: **tighten management RBAC + lockdown toggle + merge + usage counts** | P2 | No | OPEN | **Known limitation accepted for MVP (ADR-0018):** `canManageLabels` = ADMIN *or* LEAD-of-any-project, but any member can self-grant LEAD by creating a throwaway project → can then rename/delete shared labels. Clean fix = **ADMIN-only management** (one-line policy change). Deliberately shipping the hybrid to MVP to learn if governance needs centralizing; revisit with real usage. (F-1 tenant isolation is unaffected — this is governance, not a leak.) Also here: creation-lockdown toggle, label merge, usage counts. |
| FUT-17 | Components: **board swimlanes + component lead as watcher** | P4 | No | OPEN | Group the board by component; subscribe the component lead to its issues once Notifications lands. Deferred (ADR-0018). |
| FUT-18 | Search: **fuzzy/typo matching + weighted ranking** | P3 | No | OPEN | MVP is prefix `tsquery` + `ts_rank` (ADR-0021). Deferred: `pg_trgm` trigram fuzzy matching for typos, `setweight`/`ts_rank_cd` tuning (title > description). Additive behind the `SearchRepository` seam — no service/UI change. |
| FUT-19 | Search: **widen scope (comments, attachments, labels, users) + results page + saved searches** | P3 | No | OPEN | MVP searches issue title/description/key + project name/key only, capped 5+5 in the palette (ADR-0021). Deferred: search comment bodies, attachment filenames, labels, and the org user directory; a full paginated results **page** with filters; saved searches. Each is an additive repository method / DTO extension. |
| FUT-20 | Search: **external engine adapter** (Elastic/Typesense/Meilisearch) | P4 | No | PARKED (scale) | Postgres FTS is the V1 engine (ADR-0021 §1, PRD §5 non-goal). If search outgrows it, implement the `SearchRepository` interface against an external engine — a new class + index-sync pipeline, no change above the seam. Needs its own ADR + cost estimate. |
| FUT-21 | Admin: **delegated / custom admin scopes** (billing-admin, audit-viewer, custom roles) | P3 | No | OPEN | Control plane built for it (ADR-0022 §1): capabilities resolve via `resolveCapabilities(actor)`, today ADMIN→all. Delegated scopes / custom roles land by extending that ONE function — no admin service or route change. Ties to `15_roles.md` Future Scope. |
| FUT-22 | Admin: **feature-flag targeting** (percentage rollout, per-user/project, scheduling) | P3 | No | OPEN | V1 flags are org-level boolean overrides (ADR-0023 §4). Targeting = extra columns + richer evaluation inside the same `FeatureFlagService.isEnabled` seam; no call-site change. |
| FUT-23 | Admin: **org-policy config, billing, multi-org switch, impersonation, system-health, audit-log export** | P3 | No | OPEN | Each is a new console **section** (one `ADMIN_SECTIONS` registry entry + page, ADR-0022 §3) and/or a capability. Dynamic policy needs the deferred `OrgSetting` store. Multi-org = V2 SaaS (ADR-0001). |
| FUT-24 | User Management: **workspaces, teams, groups** | P3 | No | OPEN | New tables (`Workspace`/`Team`/`Group`/`GroupMember`) referencing `User`+`Organization`; membership resolution plugs into the permission engine (ADR-0024) — no consuming-module change (14_user_management.md Extensibility). |
| FUT-25 | User Management: **custom roles + permission inheritance** | P3 | No | OPEN | The engine already centralizes "effective role"; custom roles become a `Role`/`Permission` table it reads, inheritance resolved in that one place. |
| FUT-26 | User Management: **provisioning — bulk CSV invite, SSO auto-link, SCIM** | P3 | No | OPEN | Invite already pre-creates rows keyed by email; SCIM is an adapter calling the same `UserManagementService` lifecycle methods — provisioning reuses the module, not duplicates it. |
| NAV-1 | Navigation: **registry + grouped global rail** (Personal/Workspace/Admin), add Inbox, bottom-anchor Admin, move account to sidebar | P2 | No | OPEN | Phase "Now" of ADR-0025 / `01_Architecture/07_Navigation_Architecture.md`. Fixes the sparse sidebar; lays the modular foundation. Keep Home only (no separate My Work — §7). |
| NAV-2 | Navigation: **contextual project rail** (Board/Backlog/Sprints/Reports/Components&Labels/Settings) via `project`-scoped registry entries + Inbox badge | P2 | No | OPEN | Phase "MVP" of ADR-0025 — the real depth jump. |
| NAV-3 | Navigation: **growth/enterprise** — collapsible/persisted sections, **My Work** (on §7 trigger), org-level Reports/Insights, org/workspace switcher, per-user pinning/customization | P3 | No | OPEN | Phases "Growth/Enterprise" of ADR-0025; all additive behind the nav registry. |
| TD-2 | Migrate legacy string audit actions to the `AuditAction` taxonomy | P4 | No | OPEN | New admin writes use the typed `AuditAction` catalog (ADR-0022 §2); existing string actions (`ISSUE_STATUS_CHANGED`, etc.) still work and migrate opportunistically — no big-bang rewrite. |
| TD-1 | Consolidate the 5 issue-list-item mappers into one shared fn | P4 | No | OPEN | `issue.service.toListDto`, `board.service.toCardDto`, `home.service.toCard`, `backlog.service.toCardDto`, `sprint.service.toCardDto` are near-identical `IssueListItemDto` mappers; extract one `toIssueListItemDto` when convenient (kept per-feature for now, consistent with existing pattern). |

## Remaining V1 modules
Tracked in `01_Development_Roadmap.md §2`. **All V1 modules are built** as of
2026-08-08 — Phase 4 (Auth, Projects, Issues, Board, Home), Phase 5 (Backlog,
Sprint, Comments incl. mentions/threads per ADR-0038, Attachments) and Phase 6
(Notifications, Reports, Search, Admin, User Management, Roles, Profile), plus
Workload/Scheduling ahead of schedule.

**Phase 4–6 is therefore closed and the program is at the Phase 7 gate
(Hardening).** What remains is not modules: it is the security review (GL-2),
the load test (GL-7), an accessibility pass and cost validation, plus the
go-live blockers above. Individual feature gaps live in the per-module `FUT-*`
and `CMT-*` rows — none of them blocks the gate.

**Sprint adds no schema change or migration** — it reuses the existing `Sprint`
table, `Issue.sprintId`/`rank`/`version`, and the `issues(projectId, sprintId, rank)`
index (ADR-0014). Prod just needs the `sprints` table to exist (part of the manual
baseline reconciliation, GL-4/DB-2).
