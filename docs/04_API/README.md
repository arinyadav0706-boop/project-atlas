# API Specification

**Status:** Pending — authored in Phase 2 (Detailed Design) as
`openapi.yaml`, generated from and consistent with `docs/03_Database/` and
the module docs in `docs/02_Modules/`. Route Handlers implemented in Phase 4+
must match this contract; never invent an endpoint that isn't specified
here first.
